__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.DS_Store
outputs/
.env
# API keys and other credentials — never commit
secrets/
# Uncompressed splat exports are >1GB (GitHub rejects >100MB) — keep local,
# track only the compact .sog bundles
3dgs_rooms/*.ply
